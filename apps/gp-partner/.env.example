# GP Partner — скопируйте в .env.local или задайте в Vercel → Environment Variables
# Локально (apps/gp-partner/.env.development уже задаёт это):
# VITE_API_URL=http://localhost:4000
# Production (Vercel):
VITE_API_URL=https://gp-api.onrender.com
