name: gp_mobile_shared
description: Shared GP mobile API, auth, and session layer for Flutter apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.3.0
  socket_io_client: ^3.0.0

